.DS_Store
.claude/
__pycache__/
*.pyc

# agent skills tooling (local dev only, not part of the deployed site)
.agents/
skills-lock.json

# working docs, not deployed
VIDEO-PORTAL-PROMPT.md
VIDEO-PORTAL-HANDOFF.md

# dev-only tuning tool (not deployed)
tools/

# npm (agentation dev tool - keep out of the static deploy)
node_modules/
